import { storiesOf } from '@storybook/react'
import { boolean, text } from '@storybook/addon-knobs'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { TileGroup } from '../../../components'

<Meta title="components|TileGroup" component={TileGroup} />

# TileGroup

## Group of tile components to use in feature page

### Two parent tiles surrounded by an ancestor tile. Each parent has a child tile of type box

<Preview withToolbar>
   <Story name="default">
      <TileGroup displaySecondTileContent />
   </Story>
</Preview>

## Props

<Props of={TileGroup} />
